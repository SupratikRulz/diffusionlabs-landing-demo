/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'photos.wellfound.com', 'media.licdn.com', 'wellfound.com', 'mantle.xyz', 'pbs.twimg.com']
    }
};

export default nextConfig;
